package user

import (
	"context"

	"github.com/alexander-kartavtsev/starship/iam/internal/model"
)

func (r *repository) Create(ctx context.Context, info *model.UserInfo) (string, error) {
	return "", nil
}
